#pragma once
#include <iostream>
#include <vector>

template<typename T>
void Swap(T& a, T& b)
{
	T tmp = a;
	a = b;
	b = tmp;
}

template<typename T>
void SortingInsertion(const size_t& size, T* arr)
{
	for (size_t i = 0; i < size; i++)
	{
		//
	}
}